<form method="post" action="{{ path('storie_delete', {'id': storie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ storie.id) }}">
    <button class="btn">Delete</button>
</form>
